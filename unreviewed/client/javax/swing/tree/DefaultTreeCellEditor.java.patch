diff -r 7baa22e6a6b3 src/share/classes/javax/swing/tree/DefaultTreeCellEditor.java
--- a/src/share/classes/javax/swing/tree/DefaultTreeCellEditor.java	Fri Jun 01 00:05:21 2012 -0700
+++ b/src/share/classes/javax/swing/tree/DefaultTreeCellEditor.java	Mon Jun 04 20:04:30 2012 +0100
@@ -585,6 +585,7 @@
      * renderer is not <code>null</code> and no font
      * has been specified the preferred height is that of the renderer.
      */
+    @SuppressWarnings("serial")
     public class DefaultTextField extends JTextField {
         /** Border to use. */
         protected Border         border;
@@ -666,6 +667,7 @@
     /**
      * Container responsible for placing the <code>editingComponent</code>.
      */
+    @SuppressWarnings("serial")
     public class EditorContainer extends Container {
         /**
          * Constructs an <code>EditorContainer</code> object.
