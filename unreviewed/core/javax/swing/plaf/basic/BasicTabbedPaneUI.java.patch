diff -r 7baa22e6a6b3 src/share/classes/javax/swing/plaf/basic/BasicTabbedPaneUI.java
--- a/src/share/classes/javax/swing/plaf/basic/BasicTabbedPaneUI.java	Fri Jun 01 00:05:21 2012 -0700
+++ b/src/share/classes/javax/swing/plaf/basic/BasicTabbedPaneUI.java	Mon Jun 04 20:03:17 2012 +0100
@@ -3448,6 +3448,9 @@
 
     }
 
+
+
+    @SuppressWarnings("serial")
     private class ScrollableTabViewport extends JViewport implements UIResource {
         public ScrollableTabViewport() {
             super();
@@ -3462,6 +3465,9 @@
         }
     }
 
+
+
+    @SuppressWarnings("serial")
     private class ScrollableTabPanel extends JPanel implements UIResource {
         public ScrollableTabPanel() {
             super(null);
@@ -3492,6 +3498,9 @@
         }
     }
 
+
+
+    @SuppressWarnings("serial")
     private class ScrollableTabButton extends BasicArrowButton implements UIResource,
                                                                             SwingConstants {
         public ScrollableTabButton(int direction) {
@@ -3817,6 +3826,9 @@
         return htmlViews;
     }
 
+
+
+    @SuppressWarnings("serial")
     private class TabContainer extends JPanel implements UIResource {
         private boolean notifyTabbedPane = true;
 
@@ -3861,6 +3873,9 @@
         }
     }
 
+
+
+    @SuppressWarnings("serial")
     private class CroppedEdge extends JPanel implements UIResource {
         private Shape shape;
         private int tabIndex;
