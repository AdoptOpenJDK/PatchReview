diff -r 0678af55d3db src/share/classes/java/util/Locale.java
--- a/src/share/classes/java/util/Locale.java	Tue Jun 05 17:11:26 2012 +0800
+++ b/src/share/classes/java/util/Locale.java	Tue Jun 05 19:59:16 2012 +0100
@@ -1945,7 +1945,7 @@
         // If we have no list patterns, compose the list in a simple,
         // non-localized way.
         if (listPattern == null || listCompositionPattern == null) {
-            StringBuffer result = new StringBuffer();
+            StringBuilder result = new StringBuilder();
             for (int i=0; i<stringList.length; ++i) {
                 if (i>0) result.append(',');
                 result.append(stringList[i]);
